{
  "name": "qbittorrent-exporter",
  "version": "0.1.6",
  "description": "export qbittorrent data to prometheus",
  "main": "src/main.go",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev" : "go run ./src",
    "dev:env" : "go run ./src -e",
    "build" : "cd src &&  go build -o ./a.out && ./a.out",
    "build:env" : "cd src && go build -o ./a.out && ./a.out -e"
  },
  "keywords": [
    "golang",
    "qbittorrent",
    "prometheus",
    "export",
    "grafana",
    "dashboard",
    "metrics"
  ],
  "author": "martabal",
  "license": "MIT"
}
